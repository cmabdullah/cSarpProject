<!--
@model cSarpProject.Models.PhotoGallary



<div class="col-sm-12">
    <div class="table-responsive">
        <table id="table" class="table table-striped table-bordered" cellspacing="0" width="100%">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Program</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var data in Model.StudentAll)
                {
                    <tr title="Click Here to See Details">
                        <td class="showDetails pointer">@data.Id</td>
                        <td class="showDetails pointer">@data.Name</td>
                        <td class="showDetails pointer">@data.Program</td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
-->
<h1>Null</h1>